
# Setup
Install packages

```bash
    composer install
    npm install

    php artisan migrate
    php artisan db:seed --class=UserSeeder

```

Run source
```bash
    php artisan serve
    npm run dev
```
